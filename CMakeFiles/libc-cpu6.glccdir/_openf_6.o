#VCPUv6

# ======== ('CODE', '_openf', code0)
def code0():
	label('_openf');
	PUSH();
	LDWI('errno');DOKEI(10);
	LDWI(-1);
	label('.5');
	tryhop(2);POP();RET()
# ======== (epilog)
code=[
	('EXPORT', '_openf'),
	('CODE', '_openf', code0),
	('IMPORT', 'errno') ]
module(code=code, name='../gigatron/libc/_openf.c', cpu=6);

# Local Variables:
# mode: python
# indent-tabs-mode: t
# End:
