#VCPUv7

# ======== ('CODE', 'rename', code0)
def code0():
	label('rename');
	PUSH();
	LDWI('errno');DOKEQ(10);
	LDWI(-1);
	label('.5');
	tryhop(2);POP();RET()
# ======== (epilog)
code=[
	('EXPORT', 'rename'),
	('CODE', 'rename', code0),
	('IMPORT', 'errno') ]
module(code=code, name='../gigatron/libc/rename.c', cpu=7);

# Local Variables:
# mode: python
# indent-tabs-mode: t
# End:
